# 奇遇长隆：游前规划与园区地图交接文档

更新日期：2026-08-07  
当前工作分支：`codex/feature-codex-work`

## 1. 工作区与启动

- 主仓库：`C:\Users\35113\Desktop\广州长隆\agent-in-changlong`
- 本次独立 Worktree：`C:\Users\35113\Desktop\广州长隆\feature-codex-work`
- 主要应用：`chimelong-pretrip-demo\`（Nuxt 4 / Vue 3 / TypeScript）
- 游前页地址：`http://localhost:3000/pretrip`

在 PowerShell 中启动：

```powershell
cd C:\Users\35113\Desktop\广州长隆\feature-codex-work\chimelong-pretrip-demo
npm exec --yes pnpm@10.28.1 dev
```

类型检查：

```powershell
npm exec --yes pnpm@10.28.1 typecheck
```

> 本机未全局安装 `pnpm`，因此使用 `npm exec --yes pnpm@10.28.1`。启动后 Nuxt 会有匿名遥测询问，选择 `No` 或 `Yes` 均不影响功能。

## 2. 本次已完成的游前规划功能

### 同行人员与票型推荐

- 支持选择：亲子家庭、情侣出游、朋友结伴、独自出游。
- 支持成人数、儿童数；每位儿童可填写年龄或身高。
- 票型规则已覆盖标准票、儿童票、1 大 1 小亲子票、买 2 大送 1 小家庭票，以及免费儿童提示。
- 输入框允许清空；身高小数会在失焦后按米转换为厘米（例如 `1.2` → `120cm`）。
- **独自出游固定为 1 位成人、0 位儿童**，不会显示人数加减与儿童信息。

相关文件：

- `chimelong-pretrip-demo/app/components/pretrip/PartyPicker.vue`
- `chimelong-pretrip-demo/app/components/pretrip/ChatAnswerPanel.vue`
- `chimelong-pretrip-demo/shared/types/pretrip.ts`
- `chimelong-pretrip-demo/app/utils/profile.ts`

### 对话页与伙伴状态

- 生成路线后保留在聊天页面，不再跳到单独的“路线就绪”页。
- 底部采用微信式输入栏，并提供位置、拍摄、语音输入入口。
- 伙伴标题与侧边形象会循环展示“营业中 / 进食中 / 睡觉中”状态。
- 已修复聊天页空白区域问题，消息区会铺满可用高度。

主要文件：`chimelong-pretrip-demo/app/components/pretrip/AgentChat.vue`、`DraggableCompanion.vue`。

## 3. 地图能力

### 地图表现与操作

- 使用本地切片底图：`chimelong-pretrip-demo/public/maps/chimelong/`。
- 支持鼠标滚轮缩放、双指缩放；默认最小缩放为 `1.15`。
- 缩放未达到 `1.3` 时禁止拖动；达到 `1.3` 后才能上下左右移动，避免初始视图被误拖走。
- 已移除两处“珑翠花园”兴趣点及额外导入的外围路网覆盖层。
- 展区圆点有扩大后的透明点击热区，点击名称周边也可触发。

主要文件：`chimelong-pretrip-demo/app/components/map/ParkRasterMap.vue`。

### 展区信息

- 点击熊猫村、长颈鹿、考拉、大象、老虎、黑猩猩等展区点位后，地图底部信息区会改为：展区介绍、动物实时状态与动态火爆指数。
- 已删除地图内重复的圆角悬浮信息卡，信息只保留在地图下方。

### 聊天导航

在路线生成后的聊天输入框中输入下列需求，然后按 **Enter**：

| 输入示例 | 返回内容 | 点选后效果 |
| --- | --- | --- |
| `我要去厕所` | 按步行距离排序的洗手间列表；含距离、步行时间、动态排队指数 | 打开地图、绘制到洗手间的路线、底部显示距离和步行时间 |
| `我要去长颈鹿展区` | 匹配的动物展区列表；含距离、步行时间 | 打开地图并绘制到展区的路线 |
| `我要去熊猫餐厅` / `我要吃饭` | 附近餐厅列表；含距离、步行时间 | 打开地图并绘制到餐厅的路线 |

说明：当前“当前位置”是为了演示而使用的熊猫区模拟坐标；路网距离和路线由本地园区图结构计算。接入真实定位后，应将 `AgentChat.vue` 中的 `demoPosition` 替换为设备定位状态。

## 4. 数据与实现位置

- 卫生间、餐厅等服务点：`chimelong-pretrip-demo/shared/data/parkServices.ts`
- 园区图结构与坐标：`chimelong-pretrip-demo/shared/data/parkGeometry.generated.ts`
- 最短步行路网计算：`chimelong-pretrip-demo/shared/utils/parkGeo.ts`
- 地图服务点与导航线渲染：`chimelong-pretrip-demo/app/components/map/ParkRasterMap.vue`
- 聊天识别、列表生成和导航入口：`chimelong-pretrip-demo/app/components/pretrip/AgentChat.vue`

## 5. 当前工作区变更（尚未提交）

本次改动均限制在当前 Worktree，尚未执行 `git commit` 或推送。主要变更文件：

- `chimelong-pretrip-demo/app/components/map/ParkRasterMap.vue`
- `chimelong-pretrip-demo/app/components/pretrip/AgentChat.vue`
- `chimelong-pretrip-demo/app/components/pretrip/ChatAnswerPanel.vue`
- `chimelong-pretrip-demo/app/components/pretrip/DraggableCompanion.vue`
- `chimelong-pretrip-demo/app/components/pretrip/PartyPicker.vue`
- `chimelong-pretrip-demo/app/components/pretrip/PretripExperience.vue`
- `chimelong-pretrip-demo/app/composables/usePretripJourney.ts`
- `chimelong-pretrip-demo/app/utils/profile.ts`
- `chimelong-pretrip-demo/shared/types/pretrip.ts`
- `qiyu-remotion-video/pnpm-workspace.yaml`（补充 workspace 根包配置）

## 6. 后续建议

1. 接入浏览器定位或小程序定位，替换当前导航的模拟位置。
2. 接入实时客流/排队接口，替换卫生间、展区火爆指数的前端演示数值。
3. 现有地图为本地静态瓦片；若需要更平滑的高倍缩放，应从原始 `广州长隆野生动物世界-02041.tif` 重新切出多级金字塔瓦片，并保持每一级一致的边界范围。
4. UI 已按手机端体验调校；提交前建议用手机尺寸（约 390×844）和桌面浏览器各走一遍完整流程。
